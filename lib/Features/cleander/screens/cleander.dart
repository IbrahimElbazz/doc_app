import 'package:flutter/material.dart';

class Cleanderpage extends StatelessWidget {
  const Cleanderpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text(
        "cleander",
        style: TextStyle(fontSize: 30),
      )),
    );
  }
}
